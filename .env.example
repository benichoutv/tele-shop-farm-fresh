# ==========================================
# RSLiv E-commerce Application - Environment Configuration
# ==========================================

# ------------------------------------------
# Server Configuration
# ------------------------------------------
NODE_ENV=production
PORT=3000
APP_BASE_URL=https://your-domain.com

# ------------------------------------------
# Database Configuration
# ------------------------------------------
# Type: 'sqlite' (default, recommended) or 'postgresql'
DB_TYPE=sqlite
# For SQLite (default): path to database file
DB_PATH=/var/www/rsliv/data/app.db

# For PostgreSQL (if DB_TYPE=postgresql, uncomment below):
# DATABASE_URL=postgresql://user:password@localhost:5432/rsliv

# ------------------------------------------
# File Storage
# ------------------------------------------
# Directory for product images/videos (must be writable)
UPLOAD_DIR=/var/www/rsliv/uploads

# ------------------------------------------
# Security
# ------------------------------------------
# JWT Secret - CRITICAL: Change this to a strong random string in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_IN_PRODUCTION

# ------------------------------------------
# Telegram Bot Integration
# ------------------------------------------
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
# Your Telegram username for admin contact (with @)
TELEGRAM_ADMIN_CONTACT=@your_username

# ------------------------------------------
# Frontend Configuration (for Vite build)
# ------------------------------------------
# API base URL for frontend (use /api for same-domain proxy via Nginx)
VITE_API_URL=/api
